services:
  cloud-torrent:
    build: .
    container_name: cloud-torrent
    restart: unless-stopped
    ports:
      - "8080:8080"
    environment:
      DOWNLOAD_DIR: /data/downloads
      STATE_DIR: /data/state
    volumes:
      - cloud-torrent-data:/data

volumes:
  cloud-torrent-data:
